# Hybrid Reconciliation Manager

- 2026-03-30: Scoped the work to hybrid partial-graph reconciliation, richer contradiction records, and deterministic merge validation.
- 2026-03-30: Inspecting structure-memory scan flow, bundle normalization, and current API coverage before patching behavior.
- 2026-03-30: Added hybrid partial-graph comparison for planned-vs-implemented fields, bindings, lineage, and downstream breakage summaries.
- 2026-03-30: Tightened merge validation so accepted patches cannot silently skip missing prerequisites, and stale bundles now persist rebase status.
- 2026-03-30: Verified with `PYTHONPATH=src pytest -q tests/test_structure_hybrid.py`, `PYTHONPATH=src pytest -q tests/test_api.py -k structure`, and full `PYTHONPATH=src pytest -q` (`81 passed, 2 skipped`).
- 2026-03-30: Added conservative markdown hybrid-plan ingestion, including `runtime/plans/latest.plan.md` autodiscovery without letting plan docs count as implementation evidence.
- 2026-03-30: Added bundle rebase support for API, CLI, and UI, with deterministic scan replay and review-state transfer by semantic patch key.
- 2026-03-30: Surfaced hybrid comparison and downstream breakage metrics more prominently in structure review summaries and bundle detail views.
- 2026-03-30: Verified follow-up work with `PYTHONPATH=src pytest -q tests/test_structure_hybrid.py`, `PYTHONPATH=src pytest -q tests/test_cli.py`, `PYTHONPATH=src pytest -q tests/test_api.py -k structure`, and full `PYTHONPATH=src pytest -q` (`97 passed, 3 skipped`).
- 2026-03-30: Kept stale bundle status sticky during review updates so out-of-date bundles cannot quietly lose their rebase requirement.
- 2026-03-30: Added grouped contradiction and downstream breakage summaries to reconciliation output, plus stronger CLI counters for planned-vs-implemented drift.
- 2026-03-30: Rebase now reports preserved vs dropped review states when patches disappear, so review intent stays auditable after regeneration.
- 2026-03-30: Verified this hardening pass with `PYTHONPATH=src pytest -q tests/test_structure_hybrid.py`, `PYTHONPATH=src pytest -q tests/test_cli.py`, `PYTHONPATH=src pytest -q tests/test_api.py -k structure`, and full `PYTHONPATH=src pytest -q` (`118 passed, 3 skipped`).
- 2026-03-30: Added a deterministic hybrid field matrix so each planned or observed field now lands in one stable reconciliation row with explicit status and evidence.
- 2026-03-30: Added contradiction clusters with review-aware resolution state and related patch linkage so contradiction handling is grouped and auditable instead of spread across raw records.
- 2026-03-30: Rebase now marks source bundles as superseded, carries explicit lineage to rebased bundles, and exposes rebase preview details through the CLI.
- 2026-03-30: Tightened hybrid plan/doc comparison so planned bindings can be inferred from documented sources, not only pre-materialized primary bindings.
- 2026-03-30: Verified this reconciliation pass with `PYTHONPATH=src pytest -q tests/test_structure_hybrid.py`, `PYTHONPATH=src pytest -q tests/test_cli.py`, `PYTHONPATH=src pytest -q tests/test_api.py -k structure`, and full `PYTHONPATH=src pytest -q` (`131 passed, 3 skipped`).
- 2026-03-30: Added a deterministic merge-plan artifact to bundle review state, including ordered steps, blocked steps, and explicit no-op counts before merge.
- 2026-03-30: Broadened markdown hybrid-plan ingestion for freer field/binding/input/output phrasing like `response schema`, `from ...`, singular `input`/`output`, and prefixed node references such as `table analytics.foo`.
- 2026-03-30: Surfaced contradiction-cluster resolution state and merge-plan status more prominently in bundle listings, CLI summaries, and rebase previews.
- 2026-03-30: Added regression coverage for freeform markdown ingestion, deterministic merge-plan ordering, and repeated rebase chains with superseded-bundle listing behavior.
- 2026-03-30: Verified this optional hardening pass with `PYTHONPATH=src pytest -q tests/test_structure_hybrid.py`, `PYTHONPATH=src pytest -q tests/test_cli.py`, `PYTHONPATH=src pytest -q tests/test_api.py -k structure`, and full `PYTHONPATH=src pytest -q` (`138 passed, 3 skipped`).
